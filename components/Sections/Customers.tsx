import { ReactElement } from "react";
import Container from "../Container";
import SectionHeader from "../SectionHeader";

export default function Customers(): ReactElement {
    return <Container>
        <SectionHeader>Kundenportfolio</SectionHeader>
    </Container>
}