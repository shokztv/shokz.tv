import { ReactElement } from "react";
import Container from "../Container";
import SectionHeader from "../SectionHeader";

export default function Team(): ReactElement {
    return <Container>
        <SectionHeader>Das Team</SectionHeader>
    </Container>
}